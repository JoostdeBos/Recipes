<div class="topbar">
	<% if current_user %>
		  <div class="topbar-inner">
	        <div class="container">
	          <a class="brand" href="#">Recepten</a>
	          <ul class="nav">
	            <div class="loggedIn"p>Logged in as <%= current_user.name %> <%= link_to "Log out", signout_path %></div>
	          </ul>
	        </div>
	      </div>
		<% else %>
		  		  <div class="topbar-inner">
	        <div class="container">
	          <a class="brand" href="#">Bootstrap</a>
	          <ul class="nav">
	            <li><%= link_to 'Sign in', signin_path %></li>
	          </ul>
	        </div>
	      </div>
	<% end %>
</div>